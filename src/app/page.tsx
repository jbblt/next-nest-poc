"use client";

import dotenv from "dotenv";
dotenv.config();

export default function Home() {
  return (
    <div>
      <h1>Home Page</h1>
      <span>Todo add Dashboard here</span>
    </div>
  );
}
